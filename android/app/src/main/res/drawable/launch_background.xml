<?xml version="1.0" encoding="utf-8"?>
<!--
  Launch screen shown while the process starts, before Flutter's first frame.
  The background matches the app's own background colour (see values/colors.xml)
  so the handover to the Flutter UI has no colour flash, with the Card Vault
  mark centred.
-->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/splash_background" />

    <item>
        <bitmap
            android:gravity="center"
            android:src="@mipmap/ic_launcher" />
    </item>
</layer-list>
